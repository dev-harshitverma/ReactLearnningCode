import React from "react";

const Add = (a, b) => {
    let add = a + b;
    return add;
};

const Sub = (a,b) => {
    let sub = a - b;
    return sub;
};

const Div = (a,b) => {
    let div = a / b;
    return div;
};


const Mul = (a,b) => {
    let mul = a * b;
    return mul;  
}; 



export {Add, Sub, Div, Mul};